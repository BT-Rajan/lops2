{
    "name": "bt-rajan/legalops",
    "description": "LegalOps — adds Composer only for the invoicing engine's PDF renderer (dompdf). Everything else in the app stays plain PHP/PDO, no framework.",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": ">=8.1",
        "dompdf/dompdf": "^3.0"
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    }
}
